/**
 * Document-view preload — intentionally a stub at Stage 2.
 * Stage 3 will expose the editing bridge here. For now it proves the
 * sandboxed preload pipeline works and exposes nothing to the page.
 */
console.log('redra preload alive');
